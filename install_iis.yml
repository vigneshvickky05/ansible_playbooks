---
- name: Install IIS on Windows
  hosts: windows  # Target the Windows group
  tasks:
    - name: Install IIS
      win_feature:
        name: Web-Server
        state: present

    - name: Start IIS service
      win_service:
        name: W3Svc
        state: started  # Just use 'state' to start the service
